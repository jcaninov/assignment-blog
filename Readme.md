
TODO:

[] Document classes and methods with XML comments.

[] Add tests

[] Add Error handling and validation.

[] Log errors

[] Document the architecture and design decisions in a separate markdown file.

[] Use authorization in endpoints. The POST /post endpoint could retrieve the author Id from the JWT


TODOs for later:

[] Make generic registration for ICommand and IQuery handlers in DI setup (scan assemblies for implementations and register them automatically).